# 렌더러 메시 캐시와 셰이더 파일

## 메시 재사용

- `Renderer::InitializeMeshes()`에서 삼각형·사각형·16분할 원 메시를 한 번 생성한다.
- 정점은 하나의 GPU 버퍼에 `GL_STATIC_DRAW`로 업로드하고, 메시별 시작 위치와 정점 수를 보관한다.
- `Triangle`, `Quad`, `Rect`, `Ellipse`, `Line`은 기존 메시를 사용하는 인스턴스를 등록한다.
- 인스턴스에는 화면상의 네 모서리와 색상이 들어간다. 타원은 단위 원을 위치·반경에 맞춰 변형한다.
- 프레임마다 정점이나 원의 삼각함수를 다시 계산하지 않는다. GPU에는 바뀌는 인스턴스 데이터만 보낸다.
- 인스턴스 버퍼는 용량이 부족할 때만 확장하고, 이후에는 `glBufferSubData`로 내용을 갱신한다.
- 연속된 동일 메시를 `glDrawArraysInstanced`로 묶는다. 메시 종류별로 전체 정렬하지 않아 기존 앞뒤 순서와 투명 합성을 유지한다.
- 한글 글리프의 기존 CPU 캐시를 유지한다. 글자 획은 캐시된 사각형 메시의 인스턴스로 그린다.
- 정적 메시 캐시는 렌더러 수명 동안 유지하고 종료 시 인스턴스 버퍼와 함께 해제한다.
- 메시 캐시 키에 카메라 좌표·캐릭터 위치·색상을 넣지 않는다. 이동이나 월드 확장으로 메시 개수가 늘어나지 않는다.

현재 캐시 대상은 게임이 사용하는 기본 도형 메시다. 집·캐릭터처럼 여러 도형을 조합한 객체의 그리기 명령은 매 프레임 등록한다. 동적 위치·색상까지 고정하는 캐시는 아니다.

## 독립 셰이더

모든 GLSL 소스는 `SimpleGame/Shaders` 폴더의 독립 파일로 유지한다. C++ 문자열 안에 GLSL을 넣지 않는다.

| 프로그램 | 정점 셰이더 | 프래그먼트 셰이더 |
|---|---|---|
| 도형·인스턴스·한글 | `SolidRect.vs` | `SolidRect.fs` |
| 밝기 추출·블러 | `Fullscreen.vs` | `Filter.fs` |
| HDR 톤 매핑·합성 | `Fullscreen.vs` | `PostProcess.fs` |

- `SolidRect.vs`가 정적 메시의 로컬 좌표를 인스턴스의 화면 좌표로 변환한다.
- `SolidRect.fs`의 선형 색상 변환 및 발광 처리는 그대로 사용한다.
- 공통 `Program(vertexFile, fragmentFile)` 경로로 파일 읽기·컴파일·링크를 수행한다.
- 초기화한 프로그램은 매 프레임 재사용한다. 실시간 셰이더 재로딩은 추가하지 않았다.
- 기존 MSBuild 설정이 `.vs`·`.fs` 파일을 실행 파일 옆 `Shaders` 폴더로 복사한다.
- 셰이더는 실행 파일 기준 경로로 읽는다.

## 확인 사항

빌드·실행은 사용자가 수행한다. 이번 변경은 정적 검토와 서식 확인만 수행했다.

- C++과 `SolidRect.vs`가 함께 갱신되므로 변경 후 프로젝트를 다시 빌드한다.
- 지형·집·캐릭터·원형 효과·한글 표시와 앞뒤 가림을 확인한다.
- 후처리 ON/OFF와 창 크기 변경 시 표시를 확인한다.
- 정적 메시 업로드는 초기화 때만 실행되고, 프레임마다 인스턴스 버퍼만 갱신되는지 확인할 수 있다.

인스턴싱은 전송 데이터를 줄이지만 서로 다른 메시가 번갈아 나오면 드로 호출이 늘 수 있다. 실제 성능 향상 폭은 측정하지 않았다.
